pipeline{
	agent any

		stages {
			stage('scm checkout'){
				git 'https://github.com/vivekdhake/maven-project.git'
				}
			   }
			   
		{
				stage ('compile stage'){
					steps {
					withMaven(maven : 'LocalMaven'){
					sh 'mvn compile'
				    	}
			    	}
		    	} 
					
					stage ('Testing stage'){
					steps {
					withMaven(maven : 'LocalMaven'){
					sh 'mvn test'
						}
					}
				}
				
					stage ('packaging stage'){
					steps {
					withMaven(maven : 'LocalMaven'){
					sh 'mvn package'
						}
					}
				}
				
		}
			
	}		
			
			
